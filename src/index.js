import React, { Component } from 'react';

export default class templateComponent extends Component {
    render () {
        return (
            <div>This is the template React Component</div>
        )
    }
}
